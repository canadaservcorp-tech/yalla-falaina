PORT=3000
PUBLIC_URL=http://localhost:3000
JWT_SECRET=            # node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
RESEND_API_KEY=        # optional in dev (emails print to console if empty)
MAIL_FROM=TrouvePro <onboarding@resend.dev>
PAYWALL_ENFORCED=false # false = show all providers now; true = only subscribed appear

# Phase 2 — Stripe provider subscription ($10/mo, intro coupon)
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=
STRIPE_INTRO_COUPON=   # optional: 50% off 3 months
STRIPE_WEBHOOK_SECRET=

# Phase 3 — image moderation (unset = images are NOT checked; dev only)
GOOGLE_VISION_API_KEY=
